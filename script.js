const fileUpload = document.querySelector('.winning__form-file');
        const fileInput = document.getElementById('file');

        fileUpload.addEventListener('click', () => {
            fileInput.click();
        });